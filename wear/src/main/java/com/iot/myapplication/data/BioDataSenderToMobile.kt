package com.iot.myapplication.data

import android.content.Context
import com.iot.myapplication.app.BioData

class BioDataSender(private val context:Context) {
    fun sendBioData(bioData: BioData, ) {)
}